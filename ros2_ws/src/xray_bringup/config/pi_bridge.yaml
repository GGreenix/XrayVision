pi_bridge:
  ros__parameters:
    # Hostname or IP of the Pi running the xray-station service.
    pi_host: raspberrypi.local
    pi_port: 8765
    world_frame: map
    base_frame: base_link
    camera_frame: camera_left_optical
    reconnect_delay_s: 2.0
    pose_publish_rate_hz: 2.0
